import { styled } from "@mui/material/styles";
import { Box, Typography } from "@mui/material";

export const ParentFormBox = styled(Box)({
  marginTop: 8,
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
});

export const HomeBox: React.CSSProperties = {
  margin: "auto",
  width: "100%",
  textAlign: "center",
};
